# scripts/ai_dev_assistant.py
import openai

def generate_endpoint_from_prompt(prompt: str) -> str:
    # "Добавить endpoint для массовой заморозки абонементов"
    # → генерирует router + service + test
    ...