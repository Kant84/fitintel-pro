# -*- coding: utf-8 -*-
"""E56: Помощь — инструкция по экрану (F1), чат клиентов и поддержка через MAX."""
import os
import glob
from PyQt6.QtGui import QKeySequence, QShortcut
from PyQt6.QtWidgets import (QMessageBox, QListWidget, QDialog, QVBoxLayout,
                             QHBoxLayout, QPushButton, QLabel, QApplication,
                             QTextEdit)

MAX_SUPPORT_URL = "https://max.ru/fitintel_support"  # TODO: заменить на реальный чат разработчика

HELP_TEXTS = {
    "Бухгалтерия": (
        "<h3>📊 Бухгалтерия FitIntel Pro</h3>"
        "<p><b>План счетов:</b> 50 (Касса), 51 (Банк), 57 (Эквайринг), 60 (Поставщики), "
        "62 (Клиенты), 66 (Кредиты), 90.1 (Выручка), 90.2 (Себестоимость), 91.2 (Расходы), 99 (Прибыль).</p>"
        "<h4>💰 Вкладка «Касса»</h4>"
        "<p><b>ПКО</b> — деньги в кассу (Дт 50). <b>РКО</b> — деньги из кассы (Кт 50).<br>"
        "Пример ПКО: продажа абонемента → Дт 50 Кт 90.1<br>"
        "Пример РКО: аренда зала → Дт 91.2 Кт 50</p>"
        "<h4>📊 Вкладка «Отчёты»</h4>"
        "<p><b>ОСВ</b> — оборотно-сальдовая ведомость.<br>"
        "<b>Прибыль/убыток</b> — доходы минус расходы.<br>"
        "<b>Баланс</b> — активы = пассивы (должен сходиться ✅).</p>"
        "<h4>📝 Вкладка «Проводки»</h4>"
        "<p>Журнал бухгалтерских записей (двойная запись).</p>"
        "<h4>🔄 Вкладка «1С»</h4>"
        "<p>Экспорт в CommerceML 2.0 (номенклатура, контрагенты).<br>"
        "Режим mock — для тестов без реальной 1С.</p>"
        "<p>⚠️ Настройте подключение к 1С в «Интеграции» → «1С».</p>"
    ),
    "Главная": "KPI-карточки, график выручки (зелёная линия — факт, оранжевый пунктир — AI-прогноз), тепловая карта посещаемости, топ клиентов с риском оттока. Кнопка «Веб-график» открывает интерактивный дашборд в браузере.",
    "Клиенты": "＋ Добавить — новый клиент. ✎ Изменить (или двойной клик) — правка карточки. ⊘ Деактивировать — отключить без удаления. 🗑 Удалить — с подтверждением.",
    "Абонементы": "＋ Продать абонемент — выбрать клиента и тариф. ❄ Заморозить — пауза с датой и причиной. ▶ Разморозить — снять заморозку.",
    "Входы-Выходы": "Журнал посещений с ФИО и телефоном. «Ручной вход» — регистрация визита вручную, если у клиента нет карты или Face ID.",
    "Расписание": "Расписание тренеров: кто, когда и с кем работает.",
    "Тарифы": "Тарифы клуба. «Создать тариф» — код, название, цена, длительность в днях.",
    "Платежи": "Выберите клиента → ＋ Провести платёж → ✓ Подтвердить. ↩ Возврат — полный или частичный (причина обязательна). ✕ Отмена — возврат полной суммы. ⇩ Экспорт CSV — выгрузка для бухгалтерии. Подвкладка «Проводки» — бухгалтерский журнал.",
    "Отчёты": "Календарь отчётности: сроки и статусы.",
    "Аналитика": "AI-аналитика: прогнозы, сегменты риска, веб-графики.",
    "Документы": (
        "<h3>📄 Работа с документами</h3>"
        "<p><b>Шаблоны</b> — это заготовки документов (договоры, согласия, справки). "
        "Из них система автоматически создаёт документы для каждого клиента.</p>"
        "<p><b>Переменные для шаблонов:</b><br>"
        "<code>{{client_name}}</code> — ФИО клиента<br>"
        "<code>{{phone}}</code> — телефон<br>"
        "<code>{{email}}</code> — email<br>"
        "<code>{{tariff}}</code> — название тарифа<br>"
        "<code>{{date}}</code> — сегодняшняя дата<br>"
        "<code>{{club_name}}</code> — название клуба (из White-label)</p>"
        "<p><b>Как сгенерировать документ:</b><br>"
        "1. Перейдите на вкладку <b>Документы</b><br>"
        "2. Нажмите <b>Сгенерировать</b><br>"
        "3. Выберите шаблон и клиента<br>"
        "4. Система создаст черновик</p>"
        "<p><b>Действия с документом:</b><br>"
        "• <b>Подписать</b> — переводит статус в 'подписан'<br>"
        "• <b>PDF</b> — скачивает готовый PDF<br>"
        "• <b>Печать</b> — печать таблицы</p>"
        "<p>⚠️ Для генерации нужны клиенты в разделе <b>Клиенты</b>.</p>"
    ),
    "Устройства": "Драйверы и устройства DAL. «Найти устройства» — автопоиск. «Добавить вручную»: драйвер, имя, строка подключения. IP-устройство: TCP:192.168.1.50:8080; COM-порт: COM3:9600.",
    "Пользователи": "Учётные записи сотрудников. «Создать пользователя» — логин, пароль, роль.",
    "Роли и права": "RBAC-матрица: какие права у каждой роли.",
    "Face ID": "«Движок Face ID» — статус распознавания. «Шаблоны» — зарегистрированные лица. Симуляция verify — проверка связи тестовым фото. Реальная работа — фото с камеры терминала.",
    "Лицензия": "Статус лицензии: срок действия, лимиты устройств и пользователей.",
    "Настройки": "Тема (светлая/тёмная), размер шрифта (А−/А+), проверка сервера, логи клиента.",
    "Экраны ролей": "Какие экраны видит каждая роль. Отметьте чекбоксы → «Сохранить». У superadmin этот экран скрыть нельзя.",
    "Мастер установки": "Первичная настройка: БД → лицензия → админ → клуб → устройства → тарифы.",
    "Интеграции": "API-ключи сторонних систем: 1С, Mobifitness, MAX, ЮKassa. «Настроить» — ввод ключей, «Проверить» — тест подключения. Секреты показаны как *** — оставьте *** чтобы не менять.",
    "MAX сообщения": "Чаты — переписка с клиентами. Рассылка — акции и инфо всем клиентам/тренерам или одному клиенту. Привязки — сопоставление клиента и его MAX user_id (бот пишет первым только тем, кто запустил бота). Напоминания — автоправила: абонемент истекает, клиент пропал. Журнал — статусы отправок.",
    "Оповещения": "Настройка отправки уведомлений: канал MAX, время отправки, правила (за сколько дней напоминать об истечении абонемента, через сколько дней считать клиента пропавшим) и шаблоны текстов с переменными {days} {date}. «Сгенерировать и отправить сейчас» — ручной запуск.",
    "Браслеты/Карты": (
        "📘 ИНСТРУКЦИЯ: Браслеты, карты и замки\n\n"
        "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n"
        "👤 КЛИЕНТСКАЯ КАРТА\n"
        "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n"
        "1. Введите ФИО/телефон/ID клиента → Найти\n"
        "2. 🔷 Очистить носитель — поднесите карту (станет 'Пустая карта')\n"
        "3. 💾 Привязать к клиенту — поднесите карту, система запишет UID в базу\n"
        "4. 🗑️ Сброс к заводским — полный сброс Mifare\n\n"
        "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n"
        "🔧 ПРОГРАММИРОВАНИЕ ЗАМКОВ (Kerong S80F / ITC-30)\n"
        "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n"
        "ШАГ 1 — Выберите рабочий сектор (0 по умолчанию)\n"
        "ШАГ 2 — 🛡️ Карта доступа → приложите к замку (1 писк)\n"
        "ШАГ 3 — 👑 Мастер-карта → приложите к замку\n"
        "ШАГ 4 — Введите номер шкафчика → 🔢 Назначить номер\n"
        "        → приложите к замку (замок закроется)\n"
        "        → приложите повторно (замок откроется)\n"
        "ШАГ 5 — 🔷 Очистить носитель — карта готова к выдаче клиенту\n\n"
        "КНОПКИ ПРОГРАММИРОВАНИЯ:\n"
        "  🔷 Очистить носитель — 'Пустая карта' для клиента\n"
        "  🗑️ Сброс к заводским — уничтожение утерянных карт\n"
        "  📖 Прочитать карту — диагностика (покажет все блоки)\n"
        "  🛡️ Карта доступа — авторизация замка\n"
        "  👑 Мастер-карта — админ-карта (открывает и сбрасывает клиента)\n"
        "  🔢 Назначить номер — задать номер шкафчика замку\n"
        "  🔐 Режим личный — замок открывается только этой картой\n"
        "  🚫 Очистить замок — удалить все карты из замка\n"
        "  📋 Клонировать — скопировать данные с одной карты на другую\n"
        "  🔀 Сменить сектор — перевести замок на сектор 0/1/2\n"
        "  💥 Перезагрузить замок — полный сброс (стирает номер и карты)\n\n"
        "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n"
        "☁️ ОНЛАЙН-ЗАМКИ\n"
        "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n"
        "1. Введите ID клиента (UUID)\n"
        "2. Выберите провайдера (TTLock / KERONG Cloud)\n"
        "3. Введите Lock ID из облака\n"
        "4. 💾 Создать цифровой ключ\n\n"
        "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n"
        "⚠️ ТИПИЧНЫЕ ОШИБКИ\n"
        "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n"
        "«Носитель не обнаружен» → Поднесите плотнее\n"
        "«Ошибка аутентификации» → Проверьте сектор/ключ\n"
        "«Уже привязана» → Нажмите Привязать повторно\n"
        "Считыватель не найден → Проверьте USB\n"
        "408 Timeout → Поднесите карту быстрее\n\n"
        "💡 Поддержка: MAX Messenger / Telegram"
    ),
}

DEFAULT_HELP = HELP_TEXTS["Браслеты/Карты"]


def _current_title(mw):
    lw = mw.findChild(QListWidget)
    if lw is not None and lw.currentItem() is not None:
        return lw.currentItem().text().strip()
    return ""


def show_help(mw):
    """Открывает прокручиваемое окно инструкции вместо QMessageBox."""
    title = _current_title(mw)
    text = HELP_TEXTS.get(title, DEFAULT_HELP)
    dlg = QDialog(mw)
    dlg.setWindowTitle("Инструкция: " + (title or "экран"))
    dlg.resize(750, 650)
    dlg.setMinimumSize(700, 550)
    dlg.setStyleSheet("""
        QDialog { background-color: #0f172a; }
        QTextEdit { background: #1e293b; border: 1px solid #334155; border-radius: 8px; padding: 12px; color: #e2e8f0; font-family: 'Segoe UI', sans-serif; font-size: 13px; }
        QPushButton { background: linear-gradient(135deg, #06b6d4, #0891b2); border: none; border-radius: 6px; padding: 10px 32px; font-weight: 600; color: white; }
        QPushButton:hover { opacity: 0.85; }
    """)
    v = QVBoxLayout(dlg)
    v.setContentsMargins(16, 16, 16, 16)
    te = QTextEdit()
    te.setPlainText(text)
    te.setReadOnly(True)
    v.addWidget(te, 1)
    b = QPushButton("Закрыть")
    b.clicked.connect(dlg.accept)
    v.addWidget(b)
    dlg.exec()


def open_client_chat(mw):
    lw = mw.findChild(QListWidget)
    if lw is not None:
        for i in range(lw.count()):
            if "MAX" in lw.item(i).text():
                lw.setCurrentRow(i)
                return
    QMessageBox.information(mw, "MAX", "Вкладка «MAX сообщения» появится после перезапуска клиента.")


def _logs_tail(lines=40):
    for base in (os.getcwd(), os.path.dirname(os.getcwd())):
        files = glob.glob(os.path.join(base, "logs", "client_*.log"))
        if files:
            newest = max(files, key=os.path.getmtime)
            try:
                with open(newest, encoding="utf-8", errors="replace") as f:
                    return newest + "\n" + "".join(f.readlines()[-lines:])
            except Exception:
                pass
    return "Логи не найдены"


def open_support(mw):
    dlg = QDialog(mw)
    dlg.setWindowTitle("Поддержка разработчика — MAX")
    v = QVBoxLayout(dlg)
    lbl = QLabel(
        "Вопросы и проблемы — пишите разработчику в MAX:\n" + MAX_SUPPORT_URL +
        "\n\nПеред обращением нажмите «Скопировать логи» и вставьте их в сообщение —"
        " так проблема решится быстрее.\n"
        "Что описать: экран, кнопка, что ожидали, что произошло.")
    lbl.setWordWrap(True)
    v.addWidget(lbl)
    bar = QHBoxLayout()
    b1 = QPushButton("Открыть MAX")
    b2 = QPushButton("Скопировать логи")
    b3 = QPushButton("Закрыть")
    bar.addWidget(b1); bar.addWidget(b2); bar.addWidget(b3)
    v.addLayout(bar)

    def _open():
        try:
            os.startfile(MAX_SUPPORT_URL)
        except Exception as e:
            QMessageBox.warning(dlg, "MAX", "Не удалось открыть: %s" % e)

    def _copy():
        QApplication.clipboard().setText(_logs_tail())
        b2.setText("Логи скопированы ✓")

    b1.clicked.connect(_open)
    b2.clicked.connect(_copy)
    b3.clicked.connect(dlg.accept)
    dlg.exec()


def install_help(mw):
    menu = mw.menuBar().addMenu("Помощь")
    menu.addAction("Инструкция по экрану (F1)", lambda: show_help(mw))
    menu.addAction("Чат с клиентами (MAX)", lambda: open_client_chat(mw))
    menu.addAction("Поддержка разработчика (MAX)", lambda: open_support(mw))
    sc = QShortcut(QKeySequence("F1"), mw)
    sc.activated.connect(lambda: show_help(mw))
    btn = QPushButton("❓ Инструкция")
    btn.setToolTip("Инструкция по текущему экрану (F1)")
    btn.clicked.connect(lambda: show_help(mw))
    try:
        mw.statusBar().addPermanentWidget(btn)
    except Exception:
        pass


HELP_TEXTS.update({
    "AI Центр": "AI ЦЕНТР (самообучающаяся модель оттока)\n\n1. «Обучить / дообучить» — модель учится на данных клуба (визиты, абонементы).\n2. «Список риска оттока» — клиенты с вероятностью ухода, от большей к меньшей. Позвони им первым!\n3. «Сверить прогнозы с фактом» — через 14+ дней после прогноза система проверяет себя и уточняет точность.\n4. График внизу — рост точности с каждым обучением.\n\nЧем чаще обучаешь и сверяешь — тем умнее модель.",
    "Аналитика": "АНАЛИТИКА (E16)\n\nВерхняя панель: посещаемость и выручка сегодня, риск оттока, прогноз выручки на неделю, сравнение с прошлой неделей (▲/▼).\n\n«Пересчитать историю» — собирает статистику за 56 дней (делается один раз или после импорта данных).\n«Построить прогноз» — график: голубая линия — факт, розовый пунктир — прогноз, зелёная черта — сегодня.\n\nДля прогноза нужно минимум 14 дней истории.",
})